<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/funfinder"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChatApp"
        tools:targetApi="31">

        <activity
            android:name=".activities.OyunActivity"
            android:exported="true"
            android:label="Oyun Ekranı"
            android:theme="@style/Theme.ChatApp" />
        <activity
            android:name=".activities.AcikHavaActivity"
            android:exported="true"
            android:label="Açık Hava Ekranı"
            android:theme="@style/Theme.ChatApp" />
        <activity
            android:name=".activities.SarkiActivity"
            android:exported="true"
            android:label="Şarkı Ekranı"
            android:theme="@style/Theme.ChatApp" />
        <activity
            android:name=".activities.EtkinlikActivity"
            android:exported="true"
            android:label="Etkinlik Ekranı"
            android:theme="@style/Theme.ChatApp" />
        <activity
            android:name=".activities.VideoActivity"
            android:exported="true"
            android:label="Video Ekranı"
            android:theme="@style/Theme.ChatApp" />
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="adjustResize"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ChatApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>